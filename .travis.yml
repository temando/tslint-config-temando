language: node_js
node_js: ["4"]
  
install:
  - echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" > .npmrc
  - npm install

script:
  - npm run test

deploy:
  provider: script
  script: npm publish
  on:
    tags: true
